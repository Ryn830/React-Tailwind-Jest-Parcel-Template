import * as React from 'react'
import { render } from 'react-dom'

import './index.css'

const App = () => <div className='w-screen h-screen p-10 bg-blue-500'>App</div>

render(<App />, document.getElementById('root'))
